const msxSubarraySum = require('./index');

test('msxSubarraySum function should exist', () => {
  expect(msxSubarraySum).toBeDefined();
});

test('', () => {
  expect(msxSubarraySum()).toEqual();
});
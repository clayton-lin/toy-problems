// Given an array of integers and a number, write a function that finds the
// maximum sum if a subarray with the passed number
// Return null if there is no possible subarray

const masSubarraySum = (arr, size) => {


  // time complexity: O()
  // space complexity: O()
  return null;
};

module.exports = masSubarraySum;
